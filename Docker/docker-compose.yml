# Docker/docker-compose.yml
version: '3.8'

services:
    nextjs:
        build:
            context: ..
            dockerfile: Docker/Dockerfile
        ports:
            - '3000:3000'
        environment:
            - NODE_ENV=production
