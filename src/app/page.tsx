'use client';

import React from 'react';
import { TestComponent } from '@/components';

const Home = (): React.JSX.Element => {
    return (
        <div>
            <TestComponent />
        </div>
    );
};
export default Home;
